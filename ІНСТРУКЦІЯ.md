# Media Autopost Bot

## Інструкція для покупця та передавання проєкту

Версія документа: **1.1**

Дата оновлення: **12 серпня 2026 року**

Тип продукту: **готовий Telegram-бот для автоматичного постингу медіа**

> Цей документ можна передати покупцю разом із вихідним кодом. Він пояснює можливості продукту, встановлення, керування, обслуговування та безпечне передавання новому власнику.

---

## 1. Що це за проєкт

**Media Autopost Bot** — один керуючий Telegram-бот, який автоматично публікує фотографії та відео в кілька Telegram-каналів.

У готовій конфігурації створено чотири незалежні профілі:

| Ключ профілю | Назва | Початковий стан |
| --- | --- | --- |
| `hot_girls` | `HOT_GERLSbot` | потрібен `chat_id`, пауза |
| `hot_puppy` | `Hot_puppybot / Hot_Puppy_official` | канал налаштований, пауза |
| `hot_yaoi` | `Hot_Yaoi_bot` | потрібен `chat_id`, пауза |
| `hotboys` | `Hotboys_media_bot / @HOT_BOYSES` | канал налаштований, пауза |

Профілі працюють незалежно. Для кожного каналу можна окремо налаштувати:

- Telegram `chat_id` або публічний `@username`;
- чергу фотографій і відео;
- інтервал публікації;
- українську, російську або англійську мову підписів;
- набір підписів;
- рекламний футер;
- паузу та ручну публікацію;
- адміністраторів;
- статистику й архів уже опублікованих файлів.

Кількість каналів не обмежена чотирма: власник може додавати нові профілі командою `/addchannel`.

---

## 2. Основні можливості

- Один процес і один Telegram-бот керують усіма каналами.
- Окрема медіатека, черга, архів і статистика для кожного каналу.
- Автоматична публікація за інтервалом від 1 хвилини до 7 днів.
- Ручна публікація командою або кнопкою.
- Автоматичне чергування фото та відео, якщо доступні обидва типи.
- Випадковий вибір файлу з черги.
- Перенесення успішно опублікованого файлу в архів.
- Генерація різноманітних підписів за часом доби.
- Захист від частого повторення схожих підписів.
- Підписи українською, російською та англійською мовами.
- Окремі набори підписів `default`, `hot_puppy` і `hotboys`.
- Додавання медіа через Telegram або безпосередньо в папки.
- Ролі власника та додаткових адміністраторів.
- Повідомлення власнику про нову помилку публікації.
- Логи з автоматичною ротацією.
- Автозапуск на Windows через Планувальник завдань.
- Автоматичний перезапуск після аварійного завершення.
- Безпечні шляхи: бот не працює з файлами за межами папки проєкту.

---

## 3. Що входить у комплект

```text
Media_Autopost_Bot/
├── app/                    # службовий модуль програми
├── caption_packs/          # окремі JSON-набори підписів
├── handlers/               # Telegram-команди та кнопки
├── keyboards/              # клавіатури керування
├── services/               # публікація, підписи та робота з файлами
├── utils/                  # безпека й робота з JSON
├── .env.example            # приклад секретних налаштувань
├── .gitignore              # виключення секретів і робочих даних із Git
├── bot.py                  # точка запуску
├── channels.json           # налаштування профілів каналів
├── config.py               # читання конфігурації
├── database.py             # локальне JSON-сховище
├── import_media.ps1        # копіювання медіа зі старих ботів
├── install_task.ps1        # встановлення автозапуску Windows
├── requirements.txt        # Python-залежності
├── server_start.bat        # безперервний серверний запуск
├── setup.bat               # створення середовища та встановлення пакетів
├── start.bat               # звичайний запуск із відкритою консоллю
├── README.md               # короткий старт
└── ІНСТРУКЦІЯ.md           # повна документація
```

Після першого запуску автоматично створюються робочі папки:

```text
channels/                   # черги та архіви медіа
data/                       # список адміністраторів
stats/                      # статистика публікацій
caption_history/            # історія використаних підписів
logs/                       # журнали роботи
.venv/                      # локальне середовище Python
.env                        # секретний токен і ID власника
```

### Що не повинно входити в публічний або продажний архів

- файл `.env` із токеном;
- папка `.venv`;
- реальні медіафайли в `channels/`, якщо права на них не передаються;
- `data/`, `stats/`, `caption_history/` і `logs/` попереднього власника;
- кеші `__pycache__`, `.pytest_cache`, `.ruff_cache`;
- резервні копії з особистими даними;
- старі токени, паролі, ключі або приватні посилання.

---

## 4. Системні вимоги

Рекомендована конфігурація:

- Windows 10/11 або Windows Server;
- Python **3.12** із Python Launcher `py`;
- доступ до інтернету;
- Telegram-бот, створений через `@BotFather`;
- права адміністратора бота в кожному цільовому каналі;
- приблизно 200 МБ для програми та Python-середовища без урахування медіа;
- вільне місце для черги й архіву фотографій та відео.

Бот не потребує окремої СУБД, Docker або платного API.

---

## 5. Встановлення на Windows

### Крок 1. Встановити Python

Встановіть Python 3.12 із [python.org](https://www.python.org/downloads/) і під час встановлення увімкніть:

- `Add Python to PATH`;
- `Install Python Launcher`.

Перевірка у `cmd` або PowerShell:

```powershell
py -3.12 --version
```

### Крок 2. Розпакувати проєкт

Рекомендований шлях:

```text
D:\Bots\Media_Autopost_Bot
```

Не запускайте бота безпосередньо всередині ZIP-архіву.

### Крок 3. Запустити налаштування

Запустіть:

```text
start.bat
```

Скрипт автоматично:

1. перевірить Python 3.12;
2. створить `.venv`;
3. встановить залежності;
4. створить `.env` на основі `.env.example`;
5. залишить консоль відкритою, якщо виникне помилка.

Після першого запуску закрийте бот, відкрийте `.env` і внесіть реальні дані.

---

## 6. Налаштування `.env`

Приклад:

```env
BOT_TOKEN=1234567890:REPLACE_WITH_REAL_TOKEN
OWNER_ID=123456789
ADMIN_IDS=

DEFAULT_CHANNEL_KEY=hot_puppy
AUTO_START=1
BOT_LANGUAGE=ua
TIMEZONE=Europe/Kyiv
SCHEDULER_POLL_SECONDS=20
NOTIFY_OWNER=1
```

| Параметр | Обов'язковий | Призначення |
| --- | --- | --- |
| `BOT_TOKEN` | так | токен нового бота від `@BotFather` |
| `OWNER_ID` | так | цифровий Telegram ID власника |
| `ADMIN_IDS` | ні | додаткові ID через кому |
| `DEFAULT_CHANNEL_KEY` | ні | профіль, який відкривається першим |
| `AUTO_START` | ні | `1` — увімкнути автоматичний розклад |
| `BOT_LANGUAGE` | ні | мова службового інтерфейсу; поточний інтерфейс український |
| `TIMEZONE` | ні | часовий пояс для ранкових/денних/вечірніх підписів |
| `SCHEDULER_POLL_SECONDS` | ні | частота перевірки розкладу, мінімум 5 секунд |
| `NOTIFY_OWNER` | ні | `1` — повідомляти власника про нові помилки |

`BOT_TOKEN` не можна надсилати стороннім особам або публікувати в GitHub. Якщо токен став відомий сторонній людині, його потрібно перевипустити через `@BotFather`.

---

## 7. Підготовка Telegram-бота й каналів

1. Створіть бота через `@BotFather` командою `/newbot`.
2. Скопіюйте токен у `.env`.
3. Додайте бота адміністратором кожного цільового каналу.
4. Надайте право публікувати повідомлення.
5. Для публічного каналу використовуйте `@username`.
6. Для приватного каналу використовуйте цифровий ID формату `-100...`.

Приклади налаштування каналів у Telegram-боті:

```text
/setchat hot_girls -1001234567890
/setchat hot_yaoi @public_channel
```

Після зміни `.env` або коду перезапустіть процес бота.

---

## 8. Додавання контенту

### Варіант A. Через Telegram

```text
/upload hot_puppy
```

Після цього надсилайте боту фото, відео або підтримувані медіафайли як документи. Завершення:

```text
/upload_stop
```

### Варіант B. Через папки на сервері

Копіюйте файли до потрібної черги:

```text
channels\hot_puppy\photos
channels\hot_puppy\videos
```

Підтримувані формати фото:

```text
.jpg .jpeg .png .webp
```

Підтримувані формати відео:

```text
.mp4 .mov .avi .mkv .webm
```

Фактична можливість надсилання конкретного файлу також залежить від чинних лімітів Telegram Bot API.

### Варіант C. Перенесення зі старих ботів

`import_media.ps1` копіює файли й не видаляє оригінали.

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\import_media.ps1" `
  -HotPuppySource "D:\Bots\Hot_puppybot\channels\hot_Puppy_official" `
  -HotboysSource "D:\Bots\Hotboys_media_bot\channels\HOT_BOYSES"
```

Для всіх чотирьох профілів доступні параметри:

- `-HotGirlsSource`;
- `-HotPuppySource`;
- `-HotYaoiSource`;
- `-HotboysSource`.

---

## 9. Як працює автопостинг

1. Бот перевіряє активні профілі через заданий інтервал.
2. Профілі без `chat_id`, вимкнені профілі та профілі на паузі пропускаються.
3. Бот вибирає фото або відео з черги.
4. Якщо є обидва типи медіа, він намагається чергувати їх.
5. Для вибраного файлу створюється підпис відповідною мовою.
6. Файл надсилається в Telegram-канал.
7. Після успішної публікації файл переноситься до `archive`.
8. Результат записується в статистику та лог.

Якщо публікація не вдалася, файл залишається в черзі. Бот збереже текст помилки й, якщо ввімкнено `NOTIFY_OWNER=1`, повідомить власника про нову помилку.

---

## 10. Ролі та права

| Дія | Власник | Адміністратор |
| --- | :---: | :---: |
| Перегляд каналів і статусу | ✅ | ✅ |
| Ручна публікація | ✅ | ✅ |
| Пауза та продовження | ✅ | ✅ |
| Зміна інтервалу й мови | ✅ | ✅ |
| Завантаження медіа | ✅ | ✅ |
| Перегляд статистики й архіву | ✅ | ✅ |
| Зміна `chat_id` | ✅ | ❌ |
| Зміна набору підписів і футера | ✅ | ❌ |
| Додавання або видалення каналу | ✅ | ❌ |
| Додавання або видалення адміністратора | ✅ | ❌ |

Власник визначається параметром `OWNER_ID` у `.env`. Додаткових адміністраторів можна задати в `ADMIN_IDS` або додати командами бота.

---

## 11. Повний список команд

### Загальні команди власника й адміністраторів

| Команда | Опис | Приклад |
| --- | --- | --- |
| `/start` | відкрити головне меню | `/start` |
| `/help` | показати список команд | `/help` |
| `/channels` | показати всі профілі | `/channels` |
| `/setchannel key` | вибрати активний профіль | `/setchannel hot_puppy` |
| `/status [key\|all]` | стан, інтервал і черга | `/status all` |
| `/sendnow [key\|all]` | опублікувати один файл зараз | `/sendnow hotboys` |
| `/pause [key\|all]` | призупинити автопостинг | `/pause all` |
| `/resume [key\|all]` | продовжити автопостинг | `/resume hot_puppy` |
| `/interval key minutes` | встановити інтервал 1–10080 хв | `/interval hot_puppy 60` |
| `/language key ua\|ru\|en` | змінити мову підписів | `/language hotboys en` |
| `/upload [key]` | почати завантаження медіа | `/upload hot_puppy` |
| `/upload_stop` | завершити завантаження | `/upload_stop` |
| `/stats [key]` | статистика за сьогодні й 7 днів | `/stats hotboys` |
| `/archive [key]` | кількість файлів в архіві | `/archive hot_puppy` |

### Команди лише для власника

| Команда | Опис | Приклад |
| --- | --- | --- |
| `/setchat key chat_id` | змінити цільовий канал | `/setchat hot_yaoi -1001234567890` |
| `/pack key name` | змінити набір підписів | `/pack hotboys hotboys` |
| `/footer key text` | встановити футер | `/footer hotboys Ads: @manager` |
| `/footer key off` | вимкнути футер | `/footer hotboys off` |
| `/addchannel key chat_id Назва` | додати новий профіль | `/addchannel news @my_news My News` |
| `/delchannel key` | видалити профіль із конфігурації | `/delchannel news` |
| `/addadmin user_id` | додати адміністратора | `/addadmin 123456789` |
| `/removeadmin user_id` | видалити адміністратора | `/removeadmin 123456789` |

Команда `/delchannel` не видаляє папки або медіафайли цього профілю.

---

## 12. Налаштування підписів

Вбудовані набори:

- `default` — універсальні підписи трьома мовами;
- `hot_puppy` — окремий набір для відповідного профілю;
- `hotboys` — окремий набір для відповідного профілю.

Зміна набору:

```text
/pack hot_puppy hot_puppy
/pack hotboys hotboys
```

Футер додається наприкінці кожного підпису:

```text
/footer hotboys 📢 Ads & partnerships: @manager
```

Щоб вимкнути футер:

```text
/footer hotboys off
```

Власний набір можна додати як JSON-файл у `caption_packs/`, використовуючи структуру наявних файлів. Для кожної мови `ua`, `ru`, `en` мають бути масиви:

```text
morning, day, evening, night, photo, video, cta
```

Щоб новий набір з'явився в команді `/pack`, розробнику потрібно додати його назву до дозволеного списку в `handlers/admin.py`.

---

## 13. Перший безпечний запуск

Під час передавання всі профілі рекомендується залишати на паузі.

Перевірте:

```text
/status all
```

Додайте по одному тестовому файлу до кожного потрібного профілю та виконайте:

```text
/sendnow hot_puppy
/sendnow hotboys
```

Переконайтеся, що:

- пост з'явився в правильному каналі;
- підпис і футер правильні;
- файл перемістився до архіву;
- статистика оновилася;
- старі боти вже не публікують дублікати.

Тільки після перевірки увімкніть розклад:

```text
/resume all
```

---

## 14. Автозапуск на Windows Server

Відкрийте PowerShell від імені адміністратора в папці проєкту та виконайте:

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\install_task.ps1"
```

Скрипт створить завдання:

```text
\TelegramBots\TG_Media_Autopost_Bot
```

Воно запускає `server_start.bat` від `SYSTEM`, стартує разом із Windows і перезапускає бот після зупинки.

Корисні PowerShell-команди:

```powershell
Get-ScheduledTask -TaskPath "\TelegramBots\" -TaskName "TG_Media_Autopost_Bot"
Start-ScheduledTask -TaskPath "\TelegramBots\" -TaskName "TG_Media_Autopost_Bot"
Stop-ScheduledTask -TaskPath "\TelegramBots\" -TaskName "TG_Media_Autopost_Bot"
```

Перед оновленням коду спочатку зупиніть завдання. Після заміни файлів запустіть його знову.

---

## 15. Логи, статистика й резервні копії

Основні журнали:

```text
logs\bot.log
logs\launcher.log
```

Зберігати в резервній копії потрібно:

- `.env` — окремо й у захищеному місці;
- `channels.json`;
- `channels/`;
- `caption_packs/`;
- `data/`;
- `stats/`;
- `caption_history/`;
- змінений вихідний код.

`.venv` копіювати не обов'язково — його можна відновити через `setup.bat`.

Рекомендація: робити резервну копію щонайменше раз на тиждень і перед кожним оновленням.

---

## 16. Типові проблеми

### `Python 3.12 not found`

Python 3.12 або Python Launcher не встановлено. Перевірте:

```powershell
py -3.12 --version
```

### `BOT_TOKEN не налаштовано`

Відкрийте `.env`, вставте реальний токен і перезапустіть бот.

### `OWNER_ID не налаштовано`

Укажіть цифровий Telegram ID власника в `.env`.

### Бот не відповідає користувачу

Доступ мають лише власник і адміністратори. Перевірте `OWNER_ID`, `ADMIN_IDS` або список, сформований командами `/addadmin`.

### `Не вказано chat_id`

Виконайте:

```text
/setchat profile_key @channel
```

або використайте цифровий ID `-100...`.

### `Forbidden` або `not enough rights`

Додайте бота адміністратором цільового каналу та надайте право публікувати повідомлення.

### `Черга порожня`

Додайте підтримувані файли через `/upload` або в папки `photos` і `videos` потрібного профілю.

### Публікації дублюються

Імовірно, старий медіабот продовжує працювати. Поставте новий бот на паузу, зупиніть старі процеси або завдання, виконайте один ручний тест і лише потім відновіть розклад.

### Файл опубліковано, але не перенесено в архів

Перевірте права облікового запису Windows на папку проєкту, вільне місце на диску та `logs\bot.log`.

### Після зміни `.env` нічого не змінилося

Зупиніть і запустіть процес заново. `.env` читається під час старту програми.

---

## 17. Відомі обмеження поточної версії

- Бот публікує один медіафайл за одну операцію, а не альбом.
- У проєкті немає вебадмінки; керування виконується через Telegram і конфігураційні файли.
- Інтерфейс команд зараз українською, хоча підписи підтримують `ua`, `ru`, `en`.
- Зберігання локальне у JSON-файлах, тому ця версія розрахована на один запущений екземпляр програми.
- Бот не завантажує контент з інших каналів автоматично.
- Бот не перевіряє авторські права на завантажені медіафайли; за законність контенту відповідає власник каналів.
- Доступність форматів і максимальний розмір файлу залежать від правил Telegram Bot API.

Ці пункти слід чесно повідомити покупцю до оплати.

---

## 18. Чекліст перед продажем

### Технічна підготовка

- [ ] Зупинити робочий процес бота.
- [ ] Перевірити синтаксис і тестовий запуск на чистому середовищі.
- [ ] Залишити всі профілі на паузі.
- [ ] Видалити `.env` із продажної копії.
- [ ] Переконатися, що `.env.example` містить лише приклади.
- [ ] Видалити `.venv`, кеші та логи.
- [ ] Видалити старі дані адміністраторів, статистику й історію.
- [ ] Не додавати `channels/`, якщо медіаконтент не є частиною угоди.
- [ ] Перевірити, що ZIP відкривається і має одну кореневу папку.
- [ ] Додати номер версії та дату збірки.

### Безпека

- [ ] Перевипустити або відкликати токен демонстраційного бота.
- [ ] Прибрати особисті ID, контакти й приватні посилання, які не передаються.
- [ ] Перевірити весь проєкт пошуком за `TOKEN`, `PASSWORD`, `SECRET`, номером телефону та email.
- [ ] Не передавати особисті сесії Telegram, cookies або файли авторизації.

### Правові й комерційні умови

- [ ] Визначити, що продається: одна копія, невиключна ліцензія чи повне передавання прав.
- [ ] Окремо погодити, чи входять канали, аудиторія, домени, бот-акаунт і медіаконтент.
- [ ] Вказати термін безкоштовної підтримки після продажу.
- [ ] Описати вартість майбутніх доробок.
- [ ] Підтвердити право на продаж усіх компонентів і медіаматеріалів.
- [ ] Зафіксувати суму, спосіб оплати та момент передавання файлів.

---

## 19. Рекомендований склад передавання покупцю

Покупцю передаються:

1. чистий ZIP із вихідним кодом;
2. файл `ІНСТРУКЦІЯ.md`;
3. файл `.env.example` без секретів;
4. список домовлених функцій;
5. номер і дата версії;
6. короткий акт приймального тестування;
7. умови ліцензії та підтримки;
8. контент, канали або бот-акаунт лише тоді, коли це прямо включено в угоду.

Рекомендовано передавати токен не в архіві, а створити нового бота на акаунті покупця. Так власник одразу контролюватиме доступ.

---

## 20. Приймальне тестування

Проєкт можна вважати переданим після виконання такого тесту:

| № | Перевірка | Очікуваний результат |
| ---: | --- | --- |
| 1 | `start.bat` на чистій системі з Python 3.12 | створено `.venv` і встановлено залежності |
| 2 | запуск із заповненим `.env` | бот відповідає власнику на `/start` |
| 3 | `/status all` | показано всі профілі без аварії |
| 4 | додавання тестового фото | файл з'явився в правильній черзі |
| 5 | `/sendnow key` | пост з'явився в правильному каналі |
| 6 | перевірка архіву | опублікований файл перенесено до `archive` |
| 7 | `/stats key` | статистика врахувала публікацію |
| 8 | `/pause key` | автоматична публікація для профілю зупинилася |
| 9 | `/resume key` | автоматична публікація відновилася |
| 10 | перезапуск Windows/завдання | бот запускається автоматично, якщо це входить у послугу |

Результати бажано зафіксувати скриншотами або коротким відео.

---

## 21. Варіанти ліцензування

Оберіть один варіант і письмово зафіксуйте його в угоді.

### Невиключна ліцензія

Покупець отримує право використовувати та змінювати свою копію. Продавець зберігає право продавати продукт іншим клієнтам.

### Виключна ліцензія

Покупець отримує ширші виключні права в погоджених межах. Такий варіант зазвичай коштує дорожче й потребує точного договору.

### Продаж із налаштуванням

Покупець отримує копію продукту, а продавець окремо виконує встановлення, підключення каналів, перенесення контенту й тестування.

> Це не юридична консультація. Для виключного передавання майнових прав або значної суми варто використати письмовий договір, підготовлений юристом.

---

## 22. Шаблон опису для оголошення

> **Media Autopost Bot — готова система автопостингу для Telegram-каналів**
>
> Один Telegram-бот керує кількома каналами з окремими чергами фото й відео, розкладом, мовами, підписами, футерами, статистикою та архівом. Доступні ручна публікація, пауза, завантаження медіа через Telegram, ролі власника й адміністраторів та автозапуск на Windows Server.
>
> У комплекті: вихідний код Python, готові скрипти встановлення й автозапуску, чотири налаштовані профілі, набори підписів і повна документація. Секрети та сторонній контент у збірку не входять.
>
> Можливе окреме встановлення на сервер, підключення каналів, перенесення контенту та подальше доопрацювання.

До оголошення бажано додати:

- скриншот меню `/channels`;
- скриншот статусу профілю;
- приклад автоматичного поста;
- коротке відео ручної публікації;
- список того, що входить у ціну;
- чіткі умови підтримки.

---

## 23. Рекомендовані майбутні покращення

- вебадмінка;
- календар публікацій;
- альбоми з кількох фото/відео;
- кнопки під постами;
- автоматичне видалення або повторне використання архіву;
- PostgreSQL для великого навантаження;
- Docker і Linux-служба;
- Telegram Mini App для керування;
- окремі часові пояси для різних каналів;
- шаблони підписів без зміни коду;
- імпорт медіа з хмарного сховища;
- аналітика реакцій і переглядів;
- резервні копії за розкладом;
- багатомовний інтерфейс керування;
- журнал дій адміністраторів;
- SaaS-режим із кількома незалежними клієнтами.

Ці функції не входять у поточну версію, якщо інше окремо не погоджено.

---

## 24. Короткий сценарій передавання новому власнику

1. Продавець передає чисту збірку без `.env` і приватного контенту.
2. Покупець створює власного Telegram-бота через `@BotFather`.
3. Покупець указує власний `BOT_TOKEN` і `OWNER_ID`.
4. Бота додають адміністратором потрібних каналів.
5. Налаштовують `chat_id`, мову, інтервал, набір підписів і футер.
6. Додають тестові медіафайли.
7. Виконують приймальне тестування.
8. Налаштовують автозапуск.
9. Старі боти зупиняють після успішного тесту.
10. Покупець підтверджує прийняття проєкту.

---

## 25. Підтримка та зміни

Перед продажем заповніть цей блок:

```text
Продавець/розробник: ______________________________
Контакт для підтримки: ____________________________
Версія переданого проєкту: ________________________
Дата передавання: __________________________________
Безкоштовна підтримка до: _________________________
Що входить у підтримку: ___________________________
Вартість додаткових робіт: ________________________
Тип ліцензії: ______________________________________
Номер договору/замовлення: ________________________
```

Рекомендовано вести зміни у форматі:

```text
Версія 1.1 — 12.08.2026
- додано повну інструкцію для покупця;
- уточнено процес безпечного передавання й приймального тесту.

Версія 1.0 — 12.08.2026
- об'єднано чотири профілі каналів в один Media Autopost Bot;
- додано окремі черги, архіви, підписи, статистику та керування;
- додано запуск і автозапуск на Windows Server.
```

---

## 26. Важливе повідомлення покупцю

Перед увімкненням автоматичної публікації перевірте правильність каналів і прав бота. Використовуйте лише контент, на який маєте законні права, та дотримуйтеся правил Telegram і законодавства своєї країни. Власник проєкту самостійно відповідає за опубліковані матеріали, рекламні повідомлення, захист токена й резервні копії.
